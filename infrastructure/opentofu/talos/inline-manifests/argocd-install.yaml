---
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: argocd-bootstrap
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  - kind: ServiceAccount
    name: argocd-bootstrap
    namespace: argocd
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argocd-bootstrap
  namespace: argocd
---
apiVersion: batch/v1
kind: Job
metadata:
  name: argocd-bootstrap
  namespace: argocd
spec:
  backoffLimit: 10
  template:
    metadata:
      labels:
        app: argocd-bootstrap
    spec:
      restartPolicy: OnFailure
      serviceAccountName: argocd-bootstrap
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: Exists
      containers:
        - name: argocd-bootstrap
          image: alpine/helm:3.16.1
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          # --- ADD ENV VARS ---
          env:
            - name: HELM_CONFIG_HOME
              value: /tmp/.config
            - name: HELM_CACHE_HOME
              value: /tmp/.cache
            - name: HELM_DATA_HOME
              value: /tmp/.local/share/helm
          # --- END ENV VARS ---
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          command:
            - sh
            - -c
            - |
              set -ex

              echo "Waiting for API server to be available..."
              until nslookup kubernetes.default.svc.cluster.local >/dev/null 2>&1; do
                echo "DNS not ready, waiting..."
                sleep 5
              done

              echo "API server is up. Adding ArgoCD Helm repo..."
              helm repo add argo https://argoproj.github.io/argo-helm
              helm repo update

              echo "Fetching ArgoCD values from GitHub..."
              curl -sL https://raw.githubusercontent.com/amillerrr/homelab-single-proxmox-node/main/clusters/production/infrastructure/argocd/values.yaml -o /tmp/argocd-values.yaml

              echo "Installing ArgoCD via Helm..."
              helm install argocd argo/argo-cd \
                --version 8.5.10 \
                --namespace argocd \
                --create-namespace \
                -f /tmp/argocd-values.yaml \
                --wait --timeout 10m

              echo "Waiting for ArgoCD deployments to be ready..."
              kubectl wait --for=condition=available --timeout=60s \
                deployment/argocd-server -n argocd

              kubectl wait --for=condition=available --timeout=60s \
                deployment/argocd-repo-server -n argocd

              kubectl wait --for=condition=available --timeout=60s \
                deployment/argocd-applicationset-controller -n argocd

              echo "ArgoCD components are ready."

              echo "Applying ApplicationSet manifests..."
              kubectl apply -f https://raw.githubusercontent.com/amillerrr/homelab-single-proxmox-node/main/clusters/production/bootstrap/appset-cluster-components.yaml
              kubectl apply -f https://raw.githubusercontent.com/amillerrr/homelab-single-proxmox-node/main/clusters/production/bootstrap/appset-cluster-components-no-prune.yaml

              echo "Bootstrap job complete."
      volumes:
        - name: tmp
          emptyDir: {}
