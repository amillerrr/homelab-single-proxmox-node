apiVersion: v1
kind: ConfigMap
metadata:
  name: cilium-config
  namespace: kube-system
data:
  enable-l2-announcements: "true"
  enable-l2-neigh-discovery: "true"
  l2-announcements-interface: "eth0"  # Adjust if your interface is different
  enable-ipv4: "true"
  kube-proxy-replacement: "strict"
